import { IAdminModel, IAdminProfileData } from "../../types";

export const AdminProfile = ({ adminData }: any) => {
  return (
    <section className="profile">
      <h1 className="profile__title">Добрый день, Босс!</h1>
    </section>
  );
};
